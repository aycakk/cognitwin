"""src.ontology — ontology infrastructure.

Houses the lazy-loaded rdflib graph and SPARQL helper used by both
the gate evaluator and build_ontology_context.

Extracted from src/services/api/pipeline.py in the Step 4 pipeline
cleanup (evaluate_all_gates extraction commit).
"""
